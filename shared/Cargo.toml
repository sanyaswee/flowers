[package]
name = "shared"
version = "0.1.0"
edition.workspace = true

[dependencies]

defmt = "1.1.1"
heapless = { version = "0.9.3", features = ["defmt", "serde"] }
serde = { version = "1", default-features = false, features = ["derive"] }
serde-json-core = "0.6.0"